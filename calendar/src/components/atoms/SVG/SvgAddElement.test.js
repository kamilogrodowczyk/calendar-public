import React from 'react';
import { render } from 'test-utils';
import SvgAddElement from './SvgAddElement';

describe('SvgAddElement', () => {
  it('Renders the component', () => {
    render(<SvgAddElement />);
  });
});
